<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The FaceBook</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Welcome to FaceBook</h1>
        <% if (result) { %>
            <div class="card">
                <div class="card-body">
                  <h5 class="card-title"><%= result.name %></h5>
                  <p class="card-text"><%= result.message %></p>
                  <p><%= result.createdAt.toLocaleDateString() %></p>
                  <a href=/edit-feed/<%= result._id%> class="btn btn-primary">Update</a>
                  <a href=/delete-feed/<%= result._id%> class="btn btn-danger">Delete</a>
                  <a href="/" class="btn btn-info">BackHomePage</a>
                </div>
              </div>
            <% } else { %>
                    there is no record
        <% } %>
    </div>
</body>
</html>